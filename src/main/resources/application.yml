spring:
  application:
    name: btg-fondos

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/btg_fondos}

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: 1800000

fund:
  initial-balance: 500000
